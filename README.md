# Customer Segmentation Using K-Means + PCA

An end-to-end Machine Learning project to segment customers using unsupervised learning (K-Means), reduce dimensionality (PCA), and serve predictions via a Flask API and Streamlit UI.

## 🚀 Deployment Guide

Follow these steps to run the complete pipeline locally:

1. **Install dependencies:**
   ```bash
   pip install -r requirements.txt